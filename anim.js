const item = {};

// Define the bounds object
const bounds = {
  x: 50,
  y: 100,
  width: 200,
  height: 150
};

// Assign the bounds to the item
item.bounds = bounds;

console.log(item);
